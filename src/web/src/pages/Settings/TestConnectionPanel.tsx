export interface TestConnectionPanelProps {
  portalId: string;
}

// Stub — replaced with the real implementation in Task 9.
export function TestConnectionPanel(_props: TestConnectionPanelProps) {
  return null;
}
