// Stub — Task 17 builds the document review UI here (header, original-document
// reference, runs list, extracted-fields table with Confirm/Apply/Dismiss).
export function DocumentDetailPage() {
  return <p>loading…</p>;
}
